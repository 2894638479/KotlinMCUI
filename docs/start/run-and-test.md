---
title: Run and test
nav_order: 2
parent: Start
---

# 运行并测试
目的是检查依赖是否被正常加载。

按照各自加载器的教程，运行你的模组。进入游戏后打开模组列表页面，在里面应该可以找到`KotlinMCUI`和`KotlinMCUI-backend`两个模组。
如果是`fabric`，需要安装`modmenu`进入模组页面。

`KotlinMCUI-backend`应该有一个配置页面的入口。进入这个配置页面，你可以看到一些测试项目。观察它的行为是否正常。

如果展示不了测试页面，请重新检查之前的步骤。